defaults:
  - model: baseline
  - writer: wandb
  - metrics: example
  - datasets: asvspoof_datasets
  - dataloader: example
  - transforms: asvspoof_transforms
  - _self_

optimizer:
  _target_: torch.optim.Adam
  lr: 0.001
  weight_decay: 0.0001

lr_scheduler:
  _target_: torch.optim.lr_scheduler.StepLR
  gamma: 0.5
  step_size: 10

loss_function:
  _target_: src.loss.example.ASoftmaxLoss
  in_feats: 128      # ← ИСПРАВИТЬ! (не 32)
  n_classes: 2       # ← Правильно
  m: 4               # ← Правильно
  s: 30.0            # ← Правильно

trainer:
  log_step: 50
  n_epochs: 10
  epoch_len: null
  device_tensors: ["data_object", "labels"]
  resume_from: null
  device: auto
  override: False
  monitor: "min val_EER"
  save_period: 5
  early_stop: 8
  save_dir: "saved"
  seed: 42
  max_grad_norm: 1.0

writer:
  project_name: "asvspoof2019_antispoofing"
  run_name: "lcnn_baseline"
  loss_names: ["loss"]
